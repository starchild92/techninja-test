{% extends 'base.html.twig' %}

{% block body %}
    <h1>Account creation</h1>
    <div class="small">By default all account types is 'saving'</div>

    <div class="col-lg-6">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Create" class="btn bnt-success" />
        {{ form_end(form) }}
    </div>
    <div class="col-lg-6">
        <div class="panel panel-info">
            <div class="panel-heading">About</div>
            <p class="panel-body">
                By default an alphanumeric 20 char string is created to be assingned to the account, it's generated by applyin' a sophisticated algorithm which uses date and time data to ensure its uniqueness.
            </p>
        </div>
    </div>

    <ul>
        <li>
            <a href="{{ path('account_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
