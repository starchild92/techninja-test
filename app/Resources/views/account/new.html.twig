{% extends 'base.html.twig' %}

{% block body %}
	<div class="page-header">
		<a class="btn btn-default pull-right" href="{{ path('account_index') }}"><i class="fa fa-arrow-left"></i> Back to the list</a>
		<h1>Account creation</h1>
	</div>

	<div>
		<div class="alert alert-info text-info"><i class="fa fa-info"></i> By default all account types is 'saving'</div>
		<div class="col-lg-6">
			{{ form_start(form) }}
			<fieldset>
				{{ form_widget(form) }}
        		<button type="submit" class="btn btn-success btn-block"><i class="fa fa-check"></i> Add a New Account</button>
			</fieldset>
			{{ form_end(form) }}
		</div>

		<div class="col-lg-6">
			<div class="panel panel-info">
				<div class="panel-heading">About</div>
				<p class="panel-body">
					By default an alphanumeric 20 char string is created to be assingned to the account, it's generated by applyin' a sophisticated algorithm which uses date and time data to ensure its uniqueness.
				</p>
			</div>
		</div>
	</div>
{% endblock %}
